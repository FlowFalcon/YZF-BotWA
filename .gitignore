node_modules/
dist/
coverage/

# Session, credentials, dan artefak WhatsApp — jangan pernah di-commit.
.auth/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env
.env.*
!.env.example
qr.png
*.log
tmp/
.runtime/

# Aset branding sumber — ditahan dari publikasi sementara. Runtime memakainya
# (`mn.png` = header menu, `rp.png` = kartu reply) tetapi menangani ketiadaannya:
# tanpa file ini menu dan reply terkirim sebagai teks polos.
lib/images/*.png

# Memori kerja agent — lokal saja, ditahan dari publikasi.
.agent/
.hermes/
