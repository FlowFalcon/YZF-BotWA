node_modules/
dist/
coverage/

# Session, credentials, dan artefak WhatsApp — jangan pernah di-commit.
.auth/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env
.env.*
!.env.example
qr.png
*.log
tmp/
.runtime/

# Aset branding sumber — ditahan dari publikasi sementara. Tidak dipakai runtime:
# thumbnail menu dibaca dari `menuThumbnailPath` (`<BOT_STORE_PATH>/assets/menu-thumbnail.jpg`).
lib/images/*.png

# Memori kerja agent — lokal saja, ditahan dari publikasi.
.agent/
.hermes/
